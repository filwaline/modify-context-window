[project]
name = "modify-context-window"
version = "0.1.0"
description = "Interactively update context_window values in Codex model JSON files."
requires-python = ">=3.11"
dependencies = [
  "rich>=13.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
